<main>
    <h1>Edit Cube</h1>
    <div class="form">
        <form action="#" method="POST">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" value="{{selectedCube.name}}">
            <label for="description">Description</label>
            <textarea id="description" name="description">{{selectedCube.description}}</textarea>
            <label for="image">ImageUrl</label>
            <input type="text" id="image" name="imageUrl" value="{{selectedCube.imageUrl}}">
            <label for="difficulty">Difficulty</label>
            <select id="difficulty" name="difficultyLevel" />
                {{#each difficultyLevels}}
                     {{>difficultyLevel}}
                {{/each}}
            </select>
            <input type="submit" value="Edit">
        </form>
    </div>
</main>